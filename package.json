{
  "name": "wdcc-go",
  "version": "0.1.0",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "dependencies": {
    "prettier": "^2.8.3"
  },
  "scripts": {
    "start-service": "yarn workspace wdcc-go-service run start",
    "test": "CI=true yarn workspaces run test"
  }
}
